# MedVisual - Sayisal Goruntu Isleme (DIP) Modulu - Python bagimliliklari
#
# Surumler bu prototipin gelistirildigi ortamla dogrulanmistir.
# Kucuk surum farklari sorun cikarmaz.

numpy==2.4.4
opencv-python==4.13.0.92
pdf2image==1.17.0
pillow==12.1.1
pytesseract==0.3.13
scikit-image==0.26.0
scipy==1.17.1
Flask==3.1.3

# --- Sistem bagimliliklari (pip ile kurulmaz) ---------------------------------
# poppler-utils : pdf2image'in PDF -> goruntu donusumu icin gereklidir
#                 (pdftoppm). Ubuntu/Debian:  sudo apt-get install poppler-utils
#                 macOS (brew):                brew install poppler
# tesseract-ocr : OCR motoru (pytesseract bunu cagirir). En az 'eng' dil paketi.
#                 Ubuntu/Debian:  sudo apt-get install tesseract-ocr
#                 macOS (brew):    brew install tesseract
#
# --- PRD'de gecen ama burada YERINE konan kutuphaneler ------------------------
# PyMuPDF (fitz)  -> pdf2image + poppler  (pdf_loader.py icindeki cagri imzasi
#                    ayni; ileride PyMuPDF'e gecis kolaydir)
# FastAPI         -> Flask                 (bu demo arayuzu icin yeterli)
# rapidfuzz       -> elle yazilmis Levenshtein (ocr.py)  (goruntu isleme dersi
#                    icin algoritmanin elle yazilmasi akademik olarak tercih
#                    edildi; istenirse rapidfuzz'a gecilebilir)
google-genai>=1.0.0
python-dotenv>=1.0.0
